plugins {
    id 'com.android.application'
}

android {
    namespace 'com.dhruvil.racer'
    compileSdk 35

    defaultConfig {
        applicationId 'com.dhruvil.racer'
        minSdk 26
        targetSdk 35
        versionCode 1
        versionName '1.0'
    }

    // The game itself lives in /web/racer so the same files also work as a website.
    sourceSets {
        main {
            assets.srcDirs = ['../web/racer']
        }
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}
